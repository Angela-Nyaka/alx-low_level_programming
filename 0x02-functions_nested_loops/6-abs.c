#include "main.h"
/**
* _abs- prints the alphabet, in lowercase, followed by a new line
*@n: int to check
* Return: absolute value
*/
int _abs(int n)
{
if (n >=  0)
{
return (n);
}
else
{
return (-n);
}
}

